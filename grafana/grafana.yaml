resources:
  - name: grafana
    type: containerservice
    attributes: 
      containers:
        - image: grafana/grafana:latest
          ports:
            - internal: 3000
              external: 3000
              schema: http